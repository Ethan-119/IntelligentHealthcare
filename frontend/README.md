# Frontend (React)

## Run locally

```bash
npm install
npm run dev
```

Default dev URL: `http://localhost:5173`

## Build

```bash
npm run build
```

Build output directory: `frontend/dist`

## Nginx reverse proxy

Production reverse proxy sample is in `nginx/default.conf`.
